import { faStackOverflow, faGithub, faLinkedin, IconDefinition } from "@fortawesome/free-brands-svg-icons";

export const networkMapping: {[key: string]: IconDefinition} = Object.freeze({
  Stackoverflow: faStackOverflow,
  GitHub: faGithub,
  LinkedIn: faLinkedin
});
